# The backend loads the repository-root .env first.
# Keep this file only for optional backend-specific overrides.
APP_ENV=development
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GMAIL_OAUTH_REDIRECT_URI=http://localhost:8000/gmail/oauth/callback
GMAIL_TOKEN_ENCRYPTION_KEY=
